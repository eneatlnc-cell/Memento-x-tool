# Memento Launcher v2.1 依赖
# 安装: pip install -r requirements.txt
# 必需
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
docker>=7.0.0
# 可选（系统托盘）
pystray>=0.19.0
Pillow>=10.0.0
# 可选（模型下载）
huggingface_hub>=0.20.0
