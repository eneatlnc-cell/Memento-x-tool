# Memento-x-tool
# Windows 启动器 — 一键安装 + 本地工具链 + 云端任务接收
#
# 安装：
#   pip install -r requirements.txt --break-system-packages
#   python installer.py

# 核心依赖
requests>=2.31.0
tqdm>=4.66.0
psutil>=5.9.0

# 本地 API 服务（从 Memento-X 迁移）
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0

# 系统托盘图标
pystray>=0.19.0
Pillow>=10.0.0

# 工具链
# PyTorch (Windows CUDA 12.4)
# torch>=2.4.0  -- 按需安装，不在此处